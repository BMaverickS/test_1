name: test_1
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # dio library
  dio: ^4.0.0
  # http library
  http: ^0.13.5
  # bloc
  flutter_bloc: '^8.1.2'
  #equatable : an abstract class that helps to implement equality without needing to explicitly 
  #            override == and hashCode.
  equatable: '^2.0.5'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
